# 2026-09-04 代码审查修复与 Electron 验证

基线：`22deabb5dfdd43977739fc36751ae25467cad3dd`。本报告所在提交包含以下五项修复。验证范围依照用户要求：修改相关模块，以及原有标准工作流的随机 20% 向上取整抽样。

## 修复结果

1. **排队保存覆盖命令结果**：preload 记录已确认状态与成功命令代次，对命令后执行的旧快照进行三方比较；保留命令数据及新增审计，同一业务集合的冲突返回 `REVISION_CONFLICT`。渲染端共享状态接收逻辑保留等待期间新增的导航审计，避免下次保存再次丢失；显式恢复仍采用恢复后的完整状态。
2. **当前通道编辑污染排队预约**：按通道状态、记录状态及所属指针确定唯一活动记录，仅更新该记录和对应子样品的结束时间。兼容缺少指针的旧数据；无法确定唯一归属时拒绝写入。
3. **导入 ID 的单引号进入内联 JavaScript**：申请及人员操作的 ID 编码补充单引号转义，保持编辑、删除和预约调用的数据含义。
4. **停用设备绕过限制**：恢复通道、预约、开始测试均在事务层检查所属设备状态，按设备名称匹配，避免名称与其他设备 ID 重名引发误拦截。
5. **新克隆缺少回归数据**：纳入十份历史合成数据的 gzip 存档；`fixtures:prepare` 根据原始 SHA-256 清单恢复缺失文件，验证已有文件，拒绝覆盖不匹配的母版。现有生成器的规范化行为已变化，因此保留原始字节而不重新生成历史数据。原清单、生成器及真实业务数据库未修改。

## 最终 Electron 验证

最终代码冻结后执行：

```powershell
npm run test:electron:review
node tests/workflows/cli.mjs --mode quick --ids S03,S08 --keep-workdir
git diff --check
```

- **相关模块：13/13 通过，0 失败、0 跳过、0 待办**。包含 12 个运行时回归，以及 1 个新克隆夹具准备与 Electron 加载回归。真实 Electron 主进程、preload、IPC 和 SQLite 参与运行；UI 用例实际点击操作。受控 IPC 延时仅位于测试中，用于确定性复现保存竞争。
- 覆盖连续导航审计保留、管理/预约命令结果持久化、失败后队列恢复、冲突业务编辑拒绝、当前/排队记录隔离、旧数据归属、非法时间原子拒绝、导入特殊 ID 编辑、停用设备恢复/预约/开始限制及名称碰撞。
- 夹具用例从仅含 Git 跟踪文件的新目录准备数据，验证幂等、清单不变、拒绝覆盖，并通过真实 Electron 加载三个源数据库。
- 运行时证据：`自动测试报告/workflows/runtime-review-1788553609635/`；总日志：`outputs-electron-review-final.log`。

## 原有标准用例随机抽样

- 总体：`tests/workflows/catalog-standard.mjs` 的 S01–S08，共 8 项；`ceil(8 × 20%) = 2` 项，即实际覆盖 25%。
- 在执行前固定随机种子 `f7d94e68bde0c055c024513832fd43ca`，按 `SHA256(seed + ':' + id)` 排序取前两项，未因执行结果更换样本。
- 目录 SHA-256：`692e5164dff7a342c7ad0c0a5538d1ed3d42c9e68c75b6760e5004baa7d1c1b8`。
- **S03 预约 TODO 取消：通过；S08 备份与恢复闭环：通过**。共 15 个既有工作流步骤；Electron 操作、数据库不变量和重启检查由原工作流执行器完成。
- 最终 Run ID：`20260904T202716965Z-eac2aaef`；证据：`自动测试报告/workflows/20260904T202716965Z-eac2aaef/artifacts/result.json`。受保护文件校验通过，变更列表为空。

## 复核及边界

独立只读代码复核没有剩余阻断项；复核发现的设备标识碰撞及预约路径后续导航审计问题均已修复，并保留先失败后通过的 Electron 日志。`git diff --check` 通过。

本次未执行全量 core 测试，未重建或验证安装包，也未替换用户当前使用的便携 EXE。测试目录及日志留在本地忽略目录；Git 中仅提交源代码、测试、合成夹具和说明文档。结论仅覆盖上述源码与验证范围。

若需撤回代码修复，可通过 `git log --oneline -- docs/verification/2026-09-04-review-fixes.md` 确认本提交，再使用 `git revert <提交SHA>` 创建反向提交；不重置历史或删除业务数据。
